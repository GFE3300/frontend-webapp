import React from 'react';
import BusinessProfileCard from './BusinessProfileCard'; // Import the separated component

/**
 * The main page component for the business profile settings.
 * It acts as a simple wrapper that renders the primary UI card.
 */
const BusinessProfilePage = () => {
    return (
        <BusinessProfileCard />
    );
};

export default BusinessProfilePage;