// src/features/venue_management/index.js

import LiveOrderDashboard from "./layout_designer/LiveOrderDashboard";

// You can export the main page component with a more feature-specific name if desired.
// For example, if this feature is always accessed as a "page".
export { LiveOrderDashboard as VenueManagementPage };

// If there were other top-level components or utilities from this feature
// that you wanted to make easily accessible, you could export them here too.
// For example:
// export { SomeOtherUtility } from './utils/someUtil';
// export { AnotherComponent } from './components/AnotherComponent';